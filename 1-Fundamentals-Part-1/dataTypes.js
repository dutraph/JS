// 1 - Number (decimal and integers)
let age = 23;

// 2 - String (text)
let firstName = "Paulo";

// 3 - Boolean (used for taking decisions {true & false})
let fullAge = true;

// 4 - Undefined (empty value)
let children;
let child = null;

// 5 - Null (empty value)

// 6 - Symbol (ES2015)

// 7 = BigInt (ES2020)

console.log(typeof age)
console.log(typeof firstName)
console.log(typeof fullAge)
console.log(typeof children)
console.log(typeof child) // return object, bit it isn't an object at all
console.log(typeof 4.2)

age = "trinta";

console.log(typeof age)