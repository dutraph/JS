// let is for variables that can be changed later or empty variables.
let age = 30;
age = 31;
let age1;

// as it name says, it's a constant
const tax = 0.4;
const birthYear = 1990;

// var is simmilar to let but out of date (never use var) discontinued since ES6
var age =40;
age = 42;
